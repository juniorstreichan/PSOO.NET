@model PSOO.Application.ViewModel.ClienteDeleteViewModel

<div class="row">
    <div class="col-sm-12">
        <div class="widget-box">
            <div class="widget-header">
                <h4 class="widget-title">Excluir Cliente</h4>
            </div>

            <div class="widget-body">
                <div class="widget-main no-padding">
                    @using (Html.BeginForm())
                    {
                        @Html.AntiForgeryToken()
                        <div class="form-horizontal">
                            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                            @Html.HiddenFor(model => model.ClienteId)
                            <br />
                            <div class="form-group">
                                @Html.LabelFor(model => model.Nome, new { @class = "control-label col-md-2" })
                                <div class="col-md-6">
                                    @Html.EditorFor(model => model.Nome, new { htmlAttributes = new { @class = "form-control", @readonly = "" } })
                                    @Html.ValidationMessageFor(model => model.Nome, "", new { @class = "text-danger" })
                                </div>
                            </div>

                            <div class="clearfix form-actions">
                                <div class="col-md-9">
                                    <button class="btn btn-minier btn-danger" type="submit" style="margin-top: 6px">
                                        <i class="ace-icon fa fa-check bigger-110"></i>
                                        Excluir
                                    </button>

                                    @Html.ActionLink("Voltar", "Index", "Cliente", "", new { @class = "btn btn-minier btn-info", @style = "margin-top: 6px;" })
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>